[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "B008",  # FastAPI's Depends() in argument defaults is the intended idiom
    "UP017", # datetime.UTC needs 3.11+; timezone.utc keeps 3.10 compatibility
]
